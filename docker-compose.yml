services:
  backend:
    build: ../asoul_calendar
    restart: always
    env_file:
      - ../asoul_calendar/.env
    volumes:
      - db_data:/data

  frontend:
    build: .
    restart: always
    network_mode: host
    environment:
      - BACKEND_URL=http://127.0.0.1:8000

volumes:
  db_data:
